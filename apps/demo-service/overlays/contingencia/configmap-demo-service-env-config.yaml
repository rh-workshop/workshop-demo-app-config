# Valores de configuración propios del sitio de CONTINGENCIA.
apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-service-config
data:
  APP_VERSION: "1.0.0"
  APP_ENTORNO: "contingencia"
  APP_MENSAJE: "Servicio de demostración — sitio de contingencia"
