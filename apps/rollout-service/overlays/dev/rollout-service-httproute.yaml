apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: rollout-service
spec:
  hostnames:
    # Sustituir por el dominio del cluster de DESARROLLO: apps.<cluster-dev>.<dominio-base>
    - rollout-dev.apps.cluster-dev.ejemplo.com
