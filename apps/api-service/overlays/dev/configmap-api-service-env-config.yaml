# Valores de configuración propios del entorno de desarrollo.
apiVersion: v1
kind: ConfigMap
metadata:
  name: api-service-config
data:
  APP_NOMBRE: "api-service"
  APP_VERSION: "1.0.0"
  APP_ENTORNO: "dev"
  APP_MENSAJE: "API de negocio del workshop — entorno de desarrollo"
